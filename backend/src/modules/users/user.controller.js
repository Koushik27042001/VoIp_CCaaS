// Users Module - To be implemented
// This will handle user management, agent profiles, etc.

export const getAgentList = async () => {
  // Get all agents
};

export const updateAgentStatus = async (agentId, status) => {
  // Update agent status (available, on_call, offline)
};

export default {
  getAgentList,
  updateAgentStatus,
};
